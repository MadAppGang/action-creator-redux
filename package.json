{
  "name": "action-creator-redux",
  "version": "1.0.1",
  "description": "A simple utility designed to make redux's action creators less verbose",
  "main": "src/index.js",
  "scripts": {
    "build": "rm -rf dist && babel src -d dist",
    "test": "jest",
    "test:w": "jest --watch",
    "test:cov": "jest --coverage"
  },
  "author": "Denys Provodnikov [dprovodnikov] MadAppGang",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "jest": "^23.6.0"
  }
}
